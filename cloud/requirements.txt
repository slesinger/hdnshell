# Web server for status API
flask
flask-cors
flask-sock
# Core dependencies
pydantic>=2.0.0
pytest>=7.0.0
requests>=2.31.0
python-dotenv>=1.0.0

bs4>=0.0.2
playwright>=1.40.0

# LLM dependencies (required for chat handler with Azure OpenAI)
langchain==1.2.0
langchain-classic==1.0.1
langchain-community==0.4.1
langchain-core==1.2.5
langchain-openai==1.1.6
langchain-text-splitters==1.1.0

# Additional LLM providers
langchain-anthropic>=0.3.0
langchain-google-genai>=2.1.0
# langchain-google-vertexai>=2.0.0  # Optional: uncomment if using Vertex AI

# LangGraph (agent orchestration, used by langchain.agents.create_agent)
langgraph==1.0.5
langgraph-checkpoint==3.0.1
langgraph-prebuilt==1.0.5
langgraph-sdk==0.3.1

# SerpAPI Search integration
google-search-results==2.4.2

# Context7 MCP integration (direct HTTP, uses requests which is already listed)

# Telegram client
telethon>=1.36.0

# RSS/Atom feed parser
feedparser>=6.0.0

# Packaging tool
pyinstaller
