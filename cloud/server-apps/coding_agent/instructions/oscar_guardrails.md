Oscar64 guardrails

Never invent or guess Oscar64, C64 Ultimate, or hardware API names. Verify candidate symbols against the available Oscar64 symbol and documentation tools before presenting them as valid.

For sprite work, remember that the image argument to spr_set is the sprite data block index at address divided by 64, not the sprite number. Keep declarations, headers, and memory assumptions consistent across files.