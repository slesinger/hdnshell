Multi-file mode

You are working in a project directory containing C and header files. Treat cross-file consistency as part of the task: declarations, includes, constants, and compile entrypoints must line up.

Use the project file tools to inspect the relevant files first. In code mode, prefer the smallest coordinated set of edits that keeps the project buildable, then compile and run when appropriate.