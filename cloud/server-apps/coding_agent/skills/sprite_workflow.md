Sprite workflow

Verify sprite APIs and examples before changing code. Track sprite data address, copy destination, and image index as one unit.

When sprite bytes move, recompute image as address divided by 64 and verify the new value against both the header documentation and a sample project before finalizing.